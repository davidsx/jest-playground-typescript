# jest-playground-typescript
A repository for typescript code testing with jest.  

Replace the code and unit test in `index.ts` and `index.test.js` file accordingly  

# Run test
```
npm install
npm test
```